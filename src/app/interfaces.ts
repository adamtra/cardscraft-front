export interface Token {
    token: string;
}

export interface User {
    
}

export interface Card {
    id: number;
    name: string;
    health: number;
    damange: number;
    manaCost: number;
    img?: Blob;
}
